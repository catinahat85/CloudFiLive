apiVersion: v1
kind: Service
metadata:
  name: ghost-app-service
  namespace: ghost
spec:
  selector:
    app: ghost-app
  ports:
  - port: 2368
    targetPort: 2368
    protocol: TCP
  type: ClusterIP
